services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-universe}
      POSTGRES_USER: ${POSTGRES_USER:-universe}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-universe} -d ${POSTGRES_DB:-universe}"]
      interval: 5s
      timeout: 5s
      retries: 20

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes", "--maxmemory-policy", "noeviction"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  migrate:
    build: .
    environment: &app-env
      APP_ENV: production
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER:-universe}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-universe}
      REDIS_URL: redis://redis:6379/0
      WECHAT_PUBLISHER_SKILL_DIR: /opt/universe-skills/weChat-autoCreate
      UNIVERSE_ANTI_AI_SKILL_DIR: /opt/universe-skills/universe-delete-ai-skill
    env_file: .env
    command: ["alembic", "upgrade", "head"]
    volumes:
      - ${LEGACY_DATA_HOST_PATH:-/opt/universe-data/legacy}:/legacy:ro
    depends_on:
      postgres:
        condition: service_healthy

  web:
    build: .
    restart: unless-stopped
    environment: *app-env
    env_file: .env
    command: ["uvicorn", "server.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "${WEB_WORKERS:-1}", "--proxy-headers"]
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ${WORKBENCH_OUTPUT_HOST_PATH:-/opt/universe-data/output}:/app/output
      - ${WECHAT_SKILL_HOST_PATH:-/opt/universe-skills/weChat-autoCreate}:/opt/universe-skills/weChat-autoCreate:ro
      - ${ANTI_AI_SKILL_HOST_PATH:-/opt/universe-skills/universe-delete-ai-skill}:/opt/universe-skills/universe-delete-ai-skill:ro
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health/ready', timeout=3).read()"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 15s
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy

  worker:
    build: .
    restart: unless-stopped
    environment: *app-env
    env_file: .env
    command: ["celery", "-A", "server.celery_app:celery_app", "worker", "-Q", "chat,text,image,external,workflow", "--loglevel=INFO", "--concurrency=${CELERY_WORKER_CONCURRENCY:-4}"]
    volumes:
      - ${WORKBENCH_OUTPUT_HOST_PATH:-/opt/universe-data/output}:/app/output
      - ${WECHAT_SKILL_HOST_PATH:-/opt/universe-skills/weChat-autoCreate}:/opt/universe-skills/weChat-autoCreate:ro
      - ${ANTI_AI_SKILL_HOST_PATH:-/opt/universe-skills/universe-delete-ai-skill}:/opt/universe-skills/universe-delete-ai-skill:ro
    healthcheck:
      test: ["CMD-SHELL", "celery -A server.celery_app:celery_app inspect ping -d celery@$$HOSTNAME --timeout=5 | grep -q pong"]
      interval: 20s
      timeout: 10s
      retries: 6
      start_period: 20s
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy

  beat:
    build: .
    restart: unless-stopped
    environment: *app-env
    env_file: .env
    command: ["celery", "-A", "server.celery_app:celery_app", "beat", "--loglevel=INFO", "--schedule=/tmp/celerybeat-schedule"]
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy

volumes:
  postgres_data:
  redis_data:
