# 公众号 Skill 适配与验收记录

## 本地实现

- 选题：执行 Bing RSS、Google News RSS 搜索，保存时间、来源与失败状态；读取 Skill 发布历史进行近30天标题去重。搜索结果为候选材料，不等于事实已经核验。
- 框架：抓取候选原文，保存素材台账，传递完整需求；用户明确的结构要求优先。
- 写作：加载实际运行目录中的 Skill 与写作参考文件；检查字数，禁止把配图计划混进正文。
- 去 AI：运行定位、可读性和保护片段脚本；模型诊断后按唯一原文片段替换，最多两轮。原稿错误的纠正与真实信息丢失分别复核；失败保留原稿。另向 Skill 评分脚本提供实际模型 L3 评分。
- 配图：第5步输出方案供确认或对话修改。确认后逐张生成、压缩、保存；重试复用已完成图片。新提交任务按ID执行，不扫描旧队列。
- 排版：实际调用 Skill 的转换器与推荐检查，按其组件库组装内联样式，运行移动排版检查。正文图插入对应段落，图片内嵌，不依赖浏览器登录后才能访问的地址。
- 预览：使用本轮会话，不重新加载旧数据库记录。修改正文后，复核、图片、排版和下载入口失效。复制提供富文本及纯文本。
- 草稿交付：调用 Skill 微信接口上传本地图片、封面及已确认HTML；要求返回草稿ID。仅写草稿箱，界面确认后执行，实际写入不在此次测试范围。

## 验证范围

39项自动测试通过。包括版本失效、去AI阻断、配图确认、指定任务执行、来源地址检查，以及模拟微信API的HTML保真上传。

实际页面在1440、1024、390宽度检查；配图方案确认和对话修改路由通过。

模型选题、框架、初稿已实际执行，但模型初稿存在无依据数字；多次复核出现不一致，稿件被阻断，不能称为从零全流程通过。

另用明确标记的人工测试稿执行真实复核、L3评分、配图方案和两张图片生成，再完成Skill排版与预览。产物位于 `output/workbench/qa-chain-20260905-v2/`，`session.json` 的 `qa_input` 说明测试输入来源。该测试未写入用户会话数据库。

浏览器确认两张图片均加载且内嵌，390宽度无横向溢出，无脚本错误。复制内容包含两张图片、内联样式及纯文本。复制检查拦截剪贴板写入，仅检查载荷；未操作真实公众号编辑器。

## 剩余外部条件

文本服务返回 HTTP 403：剩余额度不足以预扣一次请求。停止新的付费模型调用；最终排版复用同一稿件已生成的真实排版方案。补充额度后，需完成从零生成的最终回归，不能用人工测试稿的后半段结果替代。

## Skill 运行目录修复

实际使用 `WECHAT_PUBLISHER_SKILL_DIR`，Windows默认指向 `C:/Users/16972/Documents/skill/tmp/weChat-autoCreate`，不是Codex安装目录。修改了该目录的 `toolkit/recommendation_quality.py`：单独的“一定”不再被一律识别为有待补来源的事实断言；“研究表明”等实际断言检查保留。迁移到其他机器时需同步 `deploy/skill-recommendation-modal.patch`。

Skill原有 `add_draft` 存在变量拼写错误，且 `rewrite_html_images` 跳过base64图片。适配脚本使用其 `upload_image`、`upload_cover`、`add_draft_multi` 接口，保持同一确认HTML，不修改这些已安装函数。

没有部署远端、启动旧队列、充值或实际发布文章。
