services:
  - type: web
    name: universe-wechat-detection
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: REDFOX_API_KEY
        sync: false
